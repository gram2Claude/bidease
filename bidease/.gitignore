.env
__pycache__/
*.pyc
.ipynb_checkpoints/
raw_data/

# Выгрузки с боевыми данными клиента — НИКОГДА в git (репо публичный).
# Штатное место выгрузок — raw_data/ (уже игнорируется выше), но demo-notebook
# и ручные прогоны могут положить CSV рядом с кодом — страховка на этот случай.
*.csv

# Исключение: синтетические CSV-фикстуры тестов версионируем (боевых данных в них нет).
!tests/**/*.csv
